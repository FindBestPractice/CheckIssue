import styled from 'styled-components';

const Header = () => {
  return <Container>Angular / Auglar-cli</Container>;
};

export default Header;

const Container = styled.div`
  padding: 1.5rem;
  margin-bottom: 20px;
  min-width: 350px;
  font-size: x-large;
  text-align: center;
`;
